{% extends 'base.html.twig' %}

{% block title %}Watchlist{% endblock %}

{% block body %}
    <h1>Watchlist</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ watchlist.id }}</td>
            </tr>
            <tr>
                <th>Total_duration</th>
                <td>{{ watchlist.totalDuration }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_watchlist_index') }}">back to list</a>

    <a href="{{ path('app_watchlist_edit', {'id': watchlist.id}) }}">edit</a>

    {{ include('watchlist/_delete_form.html.twig') }}
{% endblock %}
