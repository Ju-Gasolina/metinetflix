<form method="post" action="{{ path('app_watchlist_item_delete', {'id': watchlist_item.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ watchlist_item.id) }}">
    <button class="btn">Delete</button>
</form>
