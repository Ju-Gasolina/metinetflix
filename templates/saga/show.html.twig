{% extends 'base.html.twig' %}

{% block title %}Saga{% endblock %}

{% block main %}
    <h1>Saga</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ saga.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ saga.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_saga_index') }}">back to list</a>

    <a href="{{ path('app_saga_edit', {'id': saga.id}) }}">edit</a>

    {{ include('saga/_delete_form.html.twig') }}
{% endblock %}
